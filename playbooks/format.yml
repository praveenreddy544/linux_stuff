---
# Author: Praveen Poreddy
- hosts: localhost
  gather_facts: no
  tasks:
    - name: format log file
      script: ../scripts/format.sh
      #args:
        #creates: /home/prav/fom
      run_once: true
